[project]
name = "jira-writeback-agent"
version = "1.0.0"
description = "Deterministic Jira write-back agent"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

